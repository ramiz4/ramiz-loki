# Base path for the application (used by Vite for asset paths)
# Default: /ramiz-loki/ (for GitHub Pages)
# For local development without a base path, use: /
# For custom deployment paths, set to your desired base path
VITE_BASE_PATH=/ramiz-loki/
